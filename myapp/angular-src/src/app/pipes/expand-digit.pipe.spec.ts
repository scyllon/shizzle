import { ExpandDigitPipe } from './expand-digit.pipe';

describe('ExpandDigitPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpandDigitPipe();
    expect(pipe).toBeTruthy();
  });
});
